s3 read

path archive
s3 copy
s3 archive

config parameter validation
validate S3 URI

holiday logic

Parse datetime string from Gene's Powershell DateTime format 
--> to my Python format

Parse from my Pyathon format --> to actually filename, considering Global "app-id.yaml"
override (if PD is not 8PM central)


add method to check if file (may be being copied) stablized